# AGENTS.md - {project_name}

## 项目目标
TODO: 描述本项目的核心目标。

## 技术栈
TODO: 列出主要技术栈和版本要求。

## 常用命令
| 命令 | 说明 |
|------|------|
| `make verify` | 运行完整验证 |

## 开发流程
- 使用 **Sisyphus** 主编排任务
- 每完成一个子任务，必须运行 `make verify` 验证

## 架构铁律
1. 保持单一职责：每个函数不超过 30 行，每个文件不超过 200 行
2. 所有核心逻辑必须有对应单元测试
3. `cli.py` / 入口层只负责参数解析和调用核心逻辑
