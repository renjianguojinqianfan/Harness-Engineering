# {project_name}

{project_description}

This is a **Harness Engineering v2** project, generated by `harness-init`. It provides a structured agent runtime with Planner, Generator, and Evaluator roles, orchestrated through a seven-stage workflow.

## Installation

```bash
pip install -e ".[dev]"
```

## CLI Commands

After installation, use the `{project_name}` CLI:

```bash
# Run an execution plan
{project_name} run [plan_path]

# Evaluate a result path
{project_name} evaluate [result_path]

# Show project status
{project_name} status
```

## Verification

Every code change must pass verification before committing:

```bash
make verify
```

This runs `ruff` linting and `pytest` with coverage enforcement (>= 85%).

## Development Commands

| Command | Description |
|---------|-------------|
| `make verify` | Run lint + tests with coverage gate |
| `make test` | Run pytest |
| `make lint` | Run ruff on `src/` |
| `make install` | Install the package in editable mode |

## Directory Structure

```
{project_name}/
├── src/{package_name}/          # Source code
│   ├── agents/                  # Planner, Generator, Evaluator
│   ├── harness/                 # Core runner and evaluator
│   └── cli.py                   # CLI entry point
├── tests/                       # Unit and integration tests
├── .harness/                    # Runtime artifacts
│   ├── plans/                   # JSON execution plans
│   └── eval_feedback/           # Evaluation output
├── docs/context.md              # Deep architecture context
└── AGENTS.md                    # Quick agent map
```

For architecture details, conventions, and common tasks, see `docs/context.md`.
