# {project_name}

Welcome to {project_name}.

## 安装

```bash
pip install -e ".[dev]"
```

## 使用

```bash
{project_name}
```

## 开发

```bash
make verify    # 运行 lint + test
make test      # 运行 pytest
make lint      # 运行 ruff
```

## 项目结构

- `src/{package_name}/` - 源码
- `tests/` - 单元测试
